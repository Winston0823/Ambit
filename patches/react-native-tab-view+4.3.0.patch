diff --git a/node_modules/react-native-tab-view/lib/module/PagerViewAdapter.js b/node_modules/react-native-tab-view/lib/module/PagerViewAdapter.js
index 398a63d..26c9ab8 100644
--- a/node_modules/react-native-tab-view/lib/module/PagerViewAdapter.js
+++ b/node_modules/react-native-tab-view/lib/module/PagerViewAdapter.js
@@ -33,6 +33,19 @@ export function PagerViewAdapter({
   React.useEffect(() => {
     navigationStateRef.current = navigationState;
   });
+  // PATCH(ambit): on Android's new-architecture pager, updating the
+  // `scrollEnabled` prop alone doesn't reliably reach the native view, leaving
+  // tab-swipe stuck in its mount-time state — so drive it imperatively too.
+  // ANDROID ONLY: the setScrollEnabledImperatively command is implemented in
+  // pager-view's Android Fabric view manager but NOT in the iOS Fabric
+  // component view — dispatching it on iOS is an unrecognized-selector native
+  // crash at mount. iOS's updateProps applies scrollEnabled correctly, so the
+  // prop path suffices there.
+  React.useEffect(() => {
+    if (Platform.OS === 'android') {
+      pagerRef.current?.setScrollEnabled?.(swipeEnabled);
+    }
+  }, [swipeEnabled]);
   const jumpTo = useLatestCallback(key => {
     const index = navigationStateRef.current.routes.findIndex(route => route.key === key);
     if (animationEnabled) {
diff --git a/node_modules/react-native-tab-view/src/PagerViewAdapter.tsx b/node_modules/react-native-tab-view/src/PagerViewAdapter.tsx
index d4a0a0e..435f86f 100644
--- a/node_modules/react-native-tab-view/src/PagerViewAdapter.tsx
+++ b/node_modules/react-native-tab-view/src/PagerViewAdapter.tsx
@@ -65,6 +65,19 @@ export function PagerViewAdapter<T extends Route>({
     navigationStateRef.current = navigationState;
   });
 
+  // PATCH(ambit): on Android's new-architecture pager, updating the
+  // `scrollEnabled` prop alone doesn't reliably reach the native view, leaving
+  // tab-swipe stuck in its mount-time state — so drive it imperatively too.
+  // ANDROID ONLY: the setScrollEnabledImperatively command is implemented in
+  // pager-view's Android Fabric view manager but NOT in the iOS Fabric
+  // component view — dispatching it on iOS is an unrecognized-selector native
+  // crash at mount. iOS's updateProps applies scrollEnabled correctly, so the
+  // prop path suffices there.
+  React.useEffect(() => {
+    if (Platform.OS === 'android') {
+      pagerRef.current?.setScrollEnabled?.(swipeEnabled);
+    }
+  }, [swipeEnabled]);
   const jumpTo = useLatestCallback((key: string) => {
     const index = navigationStateRef.current.routes.findIndex(
       (route: { key: string }) => route.key === key
