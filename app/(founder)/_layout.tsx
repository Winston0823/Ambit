// Founder route group layout. Re-uses the candidate tab shell — tabs
// navigate by name within the current group, so (founder)/feed,
// (founder)/chat, etc. are wired automatically.
export { default } from '../(candidate)/_layout';
