/// Founder-group re-export of the outer-stack chat thread (see the candidate
/// copy for why this route exists). Same screen component.
export { default } from '../../(candidate)/(tabs)/chat/[id]';
