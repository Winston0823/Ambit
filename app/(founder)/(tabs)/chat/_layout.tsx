// Re-uses the candidate stack layout, including its `unstable_settings`
// (initialRouteName: 'index') so the founder inbox stays the stack anchor
// and back never dead-ends out of the messages list.
export { default, unstable_settings } from '../../../(candidate)/(tabs)/chat/_layout';
